import {render} from '@testing-library/react';
import {describe, it} from 'vitest';

import SubscribedEmailsAdmin from './SubscribedEmailsAdmin';

describe('', () => {
  it('render', () => {
    render(<SubscribedEmailsAdmin />);
  });
});
