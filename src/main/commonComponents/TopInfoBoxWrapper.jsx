import styled from 'styled-components';

const TopInfoBoxWrapper = styled.div`
  .top-info-box-wrapper {
    display: flex;
    justify-content: space-between;

    .more-content {
      margin-left: 1em;
    }
  }
`;

export default TopInfoBoxWrapper;
